export function makeStructuredMismatchRow({
  tsUtc,
  engine,
  profile,
  locationSet,
  monthsPerYear,
  runStartYear,
  runEndYear,
  batchStartYear,
  batchEndYear,
  batchPointCount,
  pointIndex,
  year,
  month,
  location,
  latBin,
  lonBin,
  minutePg,
  expectedSigns,
  actualSigns,
  actualLongitudes1e9,
  oracleLongitudesDeg,
  mismatchMask,
}) {
  return {
    type: "mismatch_point",
    tsUtc,
    engine,
    profile,
    locationSet,
    monthsPerYear,
    runStartYear,
    runEndYear,
    batchStartYear,
    batchEndYear,
    batchPointCount,
    pointIndex,
    year,
    month,
    location,
    latBin,
    lonBin,
    minutePg,
    expectedSigns,
    actualSigns,
    actualLongitudes1e9,
    oracleLongitudesDeg,
    mismatchMask,
    planetMismatch: (mismatchMask & 0x7f) !== 0,
    ascMismatch: (mismatchMask & 0x80) !== 0,
  };
}

export function makeWindowSummaryRow({
  tsUtc,
  engine,
  profile,
  locationSet,
  monthsPerYear,
  batchYears,
  runStartYear,
  runEndYear,
  yearStart,
  yearEnd,
  pointCount,
  passCount,
  failCount,
  planetFailCount,
  ascFailCount,
  sunFailCount,
  moonFailCount,
  mercuryFailCount,
  venusFailCount,
  marsFailCount,
  jupiterFailCount,
  saturnFailCount,
  elapsedMs,
}) {
  return {
    type: "window_summary",
    tsUtc,
    engine,
    profile,
    locationSet,
    monthsPerYear,
    batchYears,
    runStartYear,
    runEndYear,
    yearStart,
    yearEnd,
    pointCount,
    passCount,
    failCount,
    planetFailCount,
    ascFailCount,
    sunFailCount,
    moonFailCount,
    mercuryFailCount,
    venusFailCount,
    marsFailCount,
    jupiterFailCount,
    saturnFailCount,
    elapsedMs,
  };
}

export function makeRandomPointResultRow({
  engine,
  seed,
  runStartYear,
  runEndYear,
  runPointCount,
  batchPoints,
  includePassingRows,
  sampleIndex,
  yearBucket,
  latStratum,
  year,
  month,
  day,
  hour,
  minute,
  minutePg,
  latBin,
  lonBin,
  expectedSigns,
  actualSigns,
  mismatchMask,
  actualLongitudes1e9,
}) {
  return {
    type: "point_result",
    engine,
    seed,
    runStartYear,
    runEndYear,
    runPointCount,
    batchPoints,
    includePassingRows,
    sampleIndex,
    yearBucket,
    latStratum,
    year,
    month,
    day,
    hour,
    minute,
    minutePg,
    latBin,
    lonBin,
    expectedSigns,
    actualSigns,
    mismatchMask,
    planetMismatch: (mismatchMask & 0x7f) !== 0,
    ascMismatch: (mismatchMask & 0x80) !== 0,
    actualLongitudes1e9,
  };
}

export function makeRandomChunkSummaryRow({
  tsUtc,
  engine,
  seed,
  runStartYear,
  runEndYear,
  runPointCount,
  batchPoints,
  includePassingRows,
  chunkIndex,
  chunkStart,
  chunkEnd,
  pointCount,
  failCount,
  planetFailCount,
  ascFailCount,
  sunFailCount,
  moonFailCount,
  mercuryFailCount,
  venusFailCount,
  marsFailCount,
  jupiterFailCount,
  saturnFailCount,
  emittedRows,
  elapsedMs,
}) {
  return {
    type: "chunk_summary",
    tsUtc,
    engine,
    seed,
    runStartYear,
    runEndYear,
    runPointCount,
    batchPoints,
    includePassingRows,
    chunkIndex,
    chunkStart,
    chunkEnd,
    pointCount,
    failCount,
    planetFailCount,
    ascFailCount,
    sunFailCount,
    moonFailCount,
    mercuryFailCount,
    venusFailCount,
    marsFailCount,
    jupiterFailCount,
    saturnFailCount,
    emittedRows,
    elapsedMs,
  };
}
